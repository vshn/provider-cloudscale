apiVersion: v1
kind: Service
metadata:
  name: {{ include "appcat-service-s3.fullname" . }}
  labels:
    {{- include "appcat-service-s3.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "appcat-service-s3.selectorLabels" . | nindent 4 }}
